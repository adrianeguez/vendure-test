import {NgModule} from '@angular/core';
import {addNavMenuSection, SharedModule, registerDashboardWidget, addActionBarItem} from '@vendure/admin-ui/core';

@NgModule({
    imports: [
        SharedModule
    ],
    providers: [
    ]
})
export class MantiAdminSharedModule {
}